apiVersion: policy.open-cluster-management.io/v1beta1
kind: OperatorPolicy
metadata:
  name: openshift-external-secrets-operator
  namespace: open-cluster-management-policies
spec:
  remediationAction: enforce
  severity: medium
  complianceType: musthave
  upgradeApproval: Automatic
  complianceConfig:
    catalogSourceUnhealthy: Compliant
    deploymentsUnavailable: NonCompliant
    upgradesAvailable: Compliant
  removalBehavior:
    clusterServiceVersions: Delete
    customResourceDefinitions: Keep
    operatorGroups: DeleteIfUnused
    subscriptions: Delete
  # operatorGroup:
  #   name: external-secrets-operator-all-namespaces
  #   namespace: openshift-operators
  #   spec: {} # Empty spec means "All Namespaces"
  operatorGroup:
    name: external-secrets-operator-group
    namespace: openshift-external-secrets-operator
    spec:
      targetNamespaces:
        - openshift-external-secrets-operator
  subscription:
    channel: stable # This is the placeholder we will patch
    name: openshift-external-secrets-operator
    namespace: openshift-external-secrets-operator
    source: redhat-operators
    sourceNamespace: openshift-marketplace
    startingCSV: PATCHME
  versions:
    - external-secrets-operator.v1.0.0  